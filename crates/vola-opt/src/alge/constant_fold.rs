use crate::DialectNode;

use super::{ConstantIndex, WkOp};

impl WkOp {
    pub(crate) fn constant_fold_nat(&self, args: &[u64]) -> Self {
        todo!()
    }
    pub(crate) fn constant_fold_scalar(&self, args: &[u64]) -> Self {
        todo!()
    }
}
