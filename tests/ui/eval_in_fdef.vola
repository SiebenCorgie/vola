//BEGIN-CONFIG
//ERROR:Result is of type "scalar<Real>", but there was no return value set.
//END-CONFIG


export fn mySubField(arg: vec3) -> real{
	let some_sub = eval sub.Sdf3d(arg);
	Sphere(1.0)
}
