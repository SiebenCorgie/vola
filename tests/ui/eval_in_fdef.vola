//BEGIN-CONFIG
//ERROR:Result is of type "scalar<Real>", but there was no return value set. This is a compiler bug!
//END-CONFIG


export fn mySubField(arg: vec3) -> real{
	let some_sub = eval sub.Sdf3d(arg);
	Sphere(1.0)
}
