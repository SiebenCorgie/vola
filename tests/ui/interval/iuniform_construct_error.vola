//BEGIN-CONFIG
//ERROR:Cannot construct "List" from interval<vec4<Real>>. Try constructing an interval from lists
//END-CONFIG

export fn call(p: real) -> vec2{
    let ai = [[[0.0; 4] .. [1.0; 4]]; 3];
    [ai.x.start, ai.y.end]
}
